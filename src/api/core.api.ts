import { IExecutableSchemaDefinition } from 'graphql-tools';
import { gql } from 'apollo-server-express';


export const CoreApi: IExecutableSchemaDefinition = {

  // ===================================================================================================================
  // Type definitions
  // ===================================================================================================================

  typeDefs: gql`
    
  `

};
